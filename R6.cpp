/*

*/

#include <iostream>
using namespace std;

int main()
{
    int a = 4;
    int b = 5;

    // Arithmetic Operators in C++
    // cout << "The value of a + b is " << a + b << endl;
    // cout << "The value of a - b is " << a - b << endl;
    // cout << "The value of a * b is " << a * b << endl;
    // cout << "The value of a / b is " << a / b << endl;
    // cout << "The value of a % b is " << a % b << endl;
    // cout << "The value of a++ is " << a++ << endl;
    // cout << "The value of a-- is " << a-- << endl;
    // cout << "The value of ++a is " << ++a << endl;
    // cout << "The value of --a  is " << --a << endl;

    // Assignment Operator in C++
    //  int z = 48;
    //  int g = 9;
    //  char f = 'k';

    // Comprasion Operator in C++
    // cout << "The value of (a == b) is " << (a == b) << endl;
    // cout << "The value of (a != b) is " << (a != b) << endl;
    // cout << "The value of (a <= b) is " << (a <= b) << endl;
    // cout << "The value of (a >= b) is " << (a >= b) << endl;
    // cout << "The value of (a < b) is " << (a < b) << endl;
    // cout << "The value of (a > b) is " << (a > b) << endl;

    // Logical Operator in C++
    cout << "The value of ((a == b)&&(a < b)) condition is " << ((a == b) && (a < b)) << endl;
    cout << "The value of ((a != b) || (a >= b)) condition is " << ((a != b) || (a >= b)) << endl;
    cout << "The value of (!(a <= b)) condition is " << (!(a <= b)) << endl;

    return 0;
}
